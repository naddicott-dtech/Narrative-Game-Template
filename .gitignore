# Godot 4+ specific ignores
.godot/
/android/

# Dev-only test harness (symlinked in by scripts/link-dev.sh) and its generated
# reports — never ship to students. The Ink runtime under /addons/inkgd is a
# real shipped dependency; only the linked GdUnit4 test addon stays private.
/test
/addons/gdUnit4
/reports/
